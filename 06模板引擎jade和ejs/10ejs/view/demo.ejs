<!doctype html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Document</title>
</head>
<body>
    <% if (type == 'admin') {%>
        <% include ./a.text%>
    <% } else {%>
        <% include ./b.text%>
    <% } %>
</body>
</html>